// This module intentionally avoids importing Firebase client SDKs so it is safe to import
// from server components. Import client-only helpers directly from their files, for example:
//  - '@/firebase/client-init' for `initializeFirebase`
//  - '@/firebase/client-provider' or '@/firebase/provider' for React hooks/components

export * from './config';
